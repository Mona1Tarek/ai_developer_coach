[project]
name = "ai_developer_coach"
version = "0.1.0"
description = "AI-powered developer coaching assistant for Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic-settings>=2.7.0",
    "openai>=1.70.0",
]

[tool.setuptools.packages.find]
where = ["app"]
